<UserControl
    x:Class="xpra.ApControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:local="clr-namespace:xpra"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300">

    <UserControl.Resources>
        <CollectionViewSource x:Key="cvs" 
                              Source="{Binding SelectedConnection.ApList}" 
                              IsLiveGroupingRequested="True"
                              >
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Display" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
        <local:DisplayToStatusConverter x:Key="displayToStatusConverter" />
        <local:DisplayToActionTextConverter x:Key="displayToActionTextConverter" />
    </UserControl.Resources>


    <Grid IsEnabled="{Binding IsWorking, Converter={local:NegateBoolConverter}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto" />
 
        </Grid.RowDefinitions>

        <ComboBox
                ItemsSource="{Binding ConnectionList}" 
                SelectedItem="{Binding SelectedConnection}"
                materialDesign:HintAssist.FloatingScale="0.50"
                Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                SelectionChanged="ComboBox_SelectionChanged"
                FontSize="24">
        </ComboBox>
        <DockPanel Grid.Row="1" Margin="0 10 0 10">
            <TextBlock DockPanel.Dock="Left"
                    Text="{Binding SelectedConnection.UrlDisplay}"
                    Margin="0,10" />
            <!--<Button Content="Settings"
                DockPanel.Dock="Right"
                Command="{Binding ShowPageCommand}"
                CommandParameter="{x:Static local:Page.Settings}"
                Style="{StaticResource MaterialDesignFlatButton}"
                HorizontalAlignment="Right"/>-->
            <Button Content="{Binding SelectedConnection.ConnectButtonText}"
                DockPanel.Dock="Right"
                Margin="0,0,10,0"
                Command="{Binding ConnectDisconnectCommand}"
                Style="{StaticResource MaterialDesignFlatButton}"
                HorizontalAlignment="Right" />
            <Button Content="CLOSE ALL APPS"
                DockPanel.Dock="Right"
                Command="{Binding CloseAllAppsCommand}"
                Style="{StaticResource MaterialDesignFlatButton}"
                HorizontalAlignment="Right"/>
        </DockPanel>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="2" Padding="0,0,20,0">
            <ItemsControl Name="appsListBox" 
                 ItemsSource="{Binding Source={StaticResource cvs}}"                 
                 HorizontalContentAlignment="Stretch">
                <ItemsControl.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <Border Padding="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Path=Name}"
                                           Padding="10"
                                           FontWeight="Bold" 
                                           />
                                        <TextBlock Text="{Binding Path=Items, Converter={StaticResource displayToStatusConverter}}"  
                                                   Grid.Column="1"
                                                   Padding="10" />
                                        <Button Content="{Binding Path=Items, Converter={StaticResource displayToActionTextConverter}}"
                                                Grid.Column="2"
                                                Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}},
                                                    Path=DataContext.AttachCommand}" 
                                                CommandParameter="{Binding Path=Name}" >
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ItemsControl.GroupStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0 0 0 20">
                            <TextBlock Text="{Binding Name}" FontSize="30" Margin="10 10 0 0"/>
                            <Border Height="2" Margin="20,5,0,0" BorderThickness="0 0 0 1"
                                BorderBrush="{DynamicResource MaterialDesignDivider}"/>
                            <Grid d:DesignWidth="384.268" d:DesignHeight="100.705"
                                  Margin="20 0 20 0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Height="Auto">
                                    <TextBlock Text="{Binding Path}" Margin="20 10 0 0"/>
                                    <TextBlock Text="{Binding Status}" Margin="20 10 0 0"/>
                                </StackPanel>
                                <Button Content="{Binding RunButtonText}"
                                    Grid.Column="1" Height="30"
                                    Background="{Binding RunButtonColor }"
                                    BorderBrush="{Binding RunButtonColor }"
                                        
                                    Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBox}},
                                        Path=DataContext.RunApCommand}" 
                                    CommandParameter="{Binding Path}" >
                                </Button>
                            </Grid>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <StackPanel Grid.Row="3">
            <TextBlock Text="{Binding Message}"
                       Visibility="{Binding IsWorking, Converter={local:BoolToVis}}"   />
            <Grid Height="20">
                <ProgressBar Height="4"
                             Visibility="{Binding IsWorking, Converter={local:BoolToVis}}"                    
                             IsIndeterminate="True"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
